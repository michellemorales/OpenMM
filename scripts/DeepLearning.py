#Michelle Morales
#Experiments with nueral networks for depression detection

import numpy as np
from keras.models import Sequential
from keras.layers.core import Dense, Activation
from keras.optimizers import SGD

model = Sequential() #initialize models
model.add(Dense(100, input_dim=?)) #add first hidden layer
model.add()
